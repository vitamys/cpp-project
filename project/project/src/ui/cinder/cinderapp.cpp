#include "cinderapp.h"

CinderApp::CinderApp():
{
    /*void setup() override {
            // Initialization code, create your GameOfLife instance here
            mGameOfLife = GameOfLife(/* initialize with necessary parameters );
        }

        void update() override {
            // Update code, called on each frame
            if (mGameOfLife.generation < mMaxGenerations && !mGameOfLife.isGridEmpty()) {
                // Update the GameOfLife instance
                mGameOfLife.updateGrid();

                // Notify Qt that the widget needs to be repainted
                widget->repaint();
            }
        }

        void draw() override {
            // Draw code, called on each frame
            // Draw your GameOfLife grid or any other visualizations
        }*/
}
